# Copy to .env locally or set these in the scheduler environment.
# Do not commit real credentials or API keys.

FTFG_DATA_ROOT=data
FTFG_ARTIFACT_ROOT=artifacts
FTFG_LOG_ROOT=artifacts/logs
FTFG_SEC_USER_AGENT="Filing-to-Factor-Gap research contact@example.com"

# WRDS should use ~/.pgpass on Amarel; do not place the password here.
WRDS_USERNAME=

# These are intentionally unused for the proposal unless a future macro appendix is requested.
FRED_API_KEY=
BLS_API_KEY=
BEA_API_KEY=
EIA_API_KEY=
